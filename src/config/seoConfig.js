import { siteConfig } from "./siteConfig";

export const seoConfig = {
  defaultTitle: `${siteConfig.name} | ${siteConfig.developer} — Software Developer`,
  defaultDescription: siteConfig.description,
};
